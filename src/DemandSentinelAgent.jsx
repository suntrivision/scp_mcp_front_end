/**
 * Demand Sentinel Agent — placeholder shell; wire data/API here when ready.
 */
export default function DemandSentinelAgent() {
  return (
    <div style={{ padding: "28px 24px", maxWidth: 900, margin: "0 auto", fontFamily: "'DM Sans','Helvetica Neue', sans-serif" }}>
      <h2 style={{ fontSize: 22, fontWeight: 700, margin: "0 0 8px", color: "#0f172a" }}>Demand Sentinel Agent</h2>
      <p style={{ fontSize: 14, color: "#64748b", margin: "0 0 20px", lineHeight: 1.6 }}>
        Monitor demand exceptions, drift, and early warnings. Connect your Y3 / frePPLe workflows here.
      </p>
      <div
        style={{
          background: "#f8fafc",
          border: "1px solid #e2e8f0",
          borderRadius: 10,
          padding: "20px 22px",
          fontSize: 13,
          color: "#475569",
          lineHeight: 1.6,
        }}
      >
        This tab is ready for your sentinel logic (alerts, thresholds, demand health). No backend call is wired yet.
      </div>
    </div>
  );
}
