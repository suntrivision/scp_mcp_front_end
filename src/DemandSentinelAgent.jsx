import DemandSentinel from './Demandsentinel.jsx';

/**
 * Demand Sentinel Agent — full dashboard UI in Demandsentinel.jsx.
 * Wire APIs / live data in that module when ready.
 */
export default function DemandSentinelAgent() {
  return <DemandSentinel />;
}
